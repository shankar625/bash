#!/bin/bash
a=20
b=30 
 if [ $a -gt $b ]; then
   echo "$a values are greater then $b"
 elif [ $a -eq $b ]; then
   echo " $a values are equal $b"
 else
   echo "$a values are lessthen $b"
 fi  
 
  
